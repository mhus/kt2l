apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kt2l-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kt2l-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kt2l-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
